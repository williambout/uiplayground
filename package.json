{
  "source": "index.html",
  "scripts": {
    "start": "parcel",
    "build": "npm config set '//registry.npmjs.org/:_authToken' ${process.env.GITHUB_ACCESS_TOKEN} && npm config set '@juliangarnierorg:registry=https://npm.pkg.github.com' && parcel build"
  },
  "dependencies": {
    "@juliangarnierorg/anime-beta": "^4.0.0-beta.89",
    "@tweakpane/plugin-essentials": "^0.2.0",
    "tweakpane": "^4.0.1"
  },
  "devDependencies": {
    "parcel": "^2.10.3"
  }
}
